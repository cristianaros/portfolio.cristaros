---
import IDELayout from "../layouts/IDELayout.astro";
import ProjectGallery from "../components/ProjectGallery";
import Terminal from "../components/Terminal";
import "../styles/global.css";
---

<IDELayout title="portfolio_dev — proyectos.json" activeFile="proyectos.json">
  <div class="flex flex-col h-full">
    <!-- Editor Content with scroll -->
    <div class="flex-1 overflow-y-auto" id="editor-content">
      <!-- Project Gallery Section -->
      <section id="projects-section">
        <ProjectGallery client:load />
      </section>

      <!-- Extra space before terminal -->
      <div class="py-12"></div>
    </div>

    <!-- Terminal (fixed at bottom) -->
    <Terminal client:visible />
  </div>
</IDELayout>
